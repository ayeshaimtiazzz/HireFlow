# HireFlow
HireFlow is a multi-tenant SaaS. Multiple companies (tenants) each sign up and get their own isolated workspace. A company called 'NovaTech' sees only its own jobs, applicants, and team. A company called 'Systems Limited' sees only its own data. They share the same database but their data is completely isolated. 
